[project]
name = "async-tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gradio>=5.0",
    "openai>=2.24.0",
    "python-dotenv>=1.2.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "anthropic>=0.40",
]

[dependency-groups]
dev = [
    "anthropic>=0.84.0",
    "pytest>=9.0.2",
]
